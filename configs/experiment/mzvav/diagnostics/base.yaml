#@package _global_

defaults:
  - override /datasource: mzvav
  - override /evaluator: classification
  - override /task_definition: diagnostics/mzvav

evaluator:
  train:
    apply_inverse_scaling: false
  val:
    apply_inverse_scaling: false
  test:
    apply_inverse_scaling: false
