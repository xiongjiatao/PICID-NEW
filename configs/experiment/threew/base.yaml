#@package _global_

defaults:
  - override /evaluator: classification
  - override /task_definition: anomaly_detection/threew
  - override /datasource: threew

evaluator:
  train:
    apply_inverse_scaling: false
  val:
    apply_inverse_scaling: false
    save_predictions: true
  test:
    apply_inverse_scaling: false
    save_predictions: true

datasource:
  include_ova: true
  task_mode: anomaly_detection
