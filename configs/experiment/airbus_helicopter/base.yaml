#@package _global_

defaults:
  - override /evaluator: classification
  - override /task_definition: anomaly_detection/airbus_helicopter
  - override /datasource: airbus_helicopter

evaluator:
  train:
    apply_inverse_scaling: false
  val:
    apply_inverse_scaling: false
    save_predictions: true
  test:
    apply_inverse_scaling: false
    save_predictions: true
