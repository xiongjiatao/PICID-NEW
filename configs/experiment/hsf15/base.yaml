#@package _global_

defaults:
  - override /evaluator: classification
  - override /task_definition: diagnostics/hsf15

evaluator:
  train:
    apply_inverse_scaling: false
  val:
    apply_inverse_scaling: false
  test:
    apply_inverse_scaling: false
