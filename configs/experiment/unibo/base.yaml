#@package _global_

defaults:
  - override /datasource: unibo21_bosello
  - override /evaluator: default
  - override /task_definition: prognostics/battery_phmd

evaluator:
  train:
    inverse_transform_name: scaler_target
    apply_inverse_scaling: True
  val:
    inverse_transform_name: scaler_target
    apply_inverse_scaling: True
  test:
    inverse_transform_name: scaler_target
    apply_inverse_scaling: True
