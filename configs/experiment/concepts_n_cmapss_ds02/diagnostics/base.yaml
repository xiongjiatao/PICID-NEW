#@package _global_

defaults:
  - override /datasource: concepts_n_cmapss_ds02
  - override /evaluator: classification
  - override /task_definition: diagnostics/concepts_n_cmapss

evaluator:
  train:
    apply_inverse_scaling: false
  val:
    apply_inverse_scaling: false
  test:
    apply_inverse_scaling: false
