_target_: picid.data.datasources.phmd_cbmv3.CBMv3Loader
fold: 0
data_name: CBMv3

# Propeller Thrust decay state coefficient (Kkt) ptdsc_port
# Propeller Torque decay state coefficient (Kkq) ptdsc_stbd
# Hull decay state coefficient (Khull) hdsc
# GT Compressor decay state coefficient (KMcompr) gcdsc
# GT Turbine decay state coefficient (KMturb) gtdsc

task_mode: hdsc # ptdsc_stbd, hdsc gcdsc gtdsc
auxiliary_tasks: [] # When we want to have fault as main task, we can use rul as auxiliary task
cache_dir: ${paths.data_dir}/phmd_cache
