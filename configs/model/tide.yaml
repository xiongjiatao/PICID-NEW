_target_: picid.model.forecasters.tide_model.TiDE_Forecaster

metadata:
  lightning_model: true

task_type: ${task_definition.task_type}
ts_in: null # must be set by the model_config
ts_out: null # must be set by the model_config
d_yc: null # must be set by the model_config
d_yt: null # must be set by the model_config
d_x: null # must be set by the model_config
d_model: 256
d_ff: 512
e_layers: 3
d_layers: 3
dropout: 0.1
freq: null
time_emb_dim: 64
use_x_dim: true

use_revin: false
use_seasonal_decomp: false
