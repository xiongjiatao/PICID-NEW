# Scripts

Script groups in `scripts/`:

- `env_setup.sh`: install environment dependencies
- `datasets_setup.sh`: fetch datasets used by experiments/tests
- `reproducibility/`: compare resolved configs and rerun from prior run outputs
- `publish/`: package and archive run artifacts

Snapshot-related helpers also live under `test/scripts/snapshot/`.
