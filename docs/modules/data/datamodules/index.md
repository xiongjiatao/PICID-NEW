# Datamodules

Datamodules convert split-specific datasets into train/val/test `DataLoader` instances.

Primary implementation: `BaseDataModule` in `picid.data.datamodules.base`.

See [Batch Contracts](batch-contracts.md).
