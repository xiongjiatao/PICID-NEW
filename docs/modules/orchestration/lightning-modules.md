# Lightning Modules

Pipeline wrappers in `picid.pipeline.base` include:

- `TrainingLightningModule`
- `FitPredictWrapperLightningModule`
- `ConstantLossLightningModule`

They standardize step execution and evaluator handoff.
