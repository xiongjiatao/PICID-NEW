# Reproducibility Artifacts

Each run should persist:

- resolved config
- checkpoint metadata
- evaluation summaries/predictions
- reproduction instructions (`REPRODUCE.md` when available)

Cache fingerprints and manifest entries provide additional provenance for preprocessing reproducibility.
