# Add a Transform

1. Implement transform class in `picid/transforms/`.
2. Choose base class and mixin strategy.
3. Add Hydra transform config with metadata routing.
4. Use `fit_on: train` for stateful transforms.
5. Validate behavior on train/val/test.

See [Transforms Module](../modules/transforms/index.md).
