# Custom Transform Tutorial

1. Implement transform class under `picid/transforms/`.
2. Configure it with `transform` + `metadata` blocks.
3. Set `apply_to`, `assign_to`, and optional `fit_on: train`.
4. Add to the transform YAML sequence and run.

See full extension guide: [Extending Transforms](../modules/transforms/extending-transforms.md).
