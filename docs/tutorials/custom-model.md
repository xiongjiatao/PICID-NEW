# Custom Model Tutorial

1. Choose wrapper type (`AbstractFitPredictWrapper` or feed-forward wrappers).
2. Implement wrapper in `picid/model/wrappers/`.
3. Register config in `configs/model_configs/` and `configs/experiment/`.
4. Run experiment and verify evaluator outputs.

See [Extending Models](../modules/modeling/extending-models.md).
