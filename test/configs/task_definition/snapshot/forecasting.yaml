seq_len: 10
label_len: 0
pred_len: 5
stride: 1
padding_left_flag: true
task_type: forecasting

model:
  data_requirements:
    input_tensors:
      - features
      - time_features
      - target

target_metric: val/loss
target_metric_mode: min
